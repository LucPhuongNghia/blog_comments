<!DOCTYPE html>
<html>
    <head></head>
    <body>
        <p>
            There's a new message from bandoc@tocdep.vn contact form.
            The message was sent from <%= "#{@name}, #{@email}" %>
        </p>
        <p>
            <%= "#{ @message }" %>
        </p>
    </body>
</html>